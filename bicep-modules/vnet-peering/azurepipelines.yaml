trigger:
- none

pool:
  vmImage: ubuntu-latest

variables: 
- group: 'Azure DevOps Variables'

extends:
  template: ../base_pipeline.yaml
  parameters:
    module_name: vnet-peering
    feed_id: '$(feed_Id)'